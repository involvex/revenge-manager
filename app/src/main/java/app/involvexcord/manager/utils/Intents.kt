package app.involvexcord.manager.utils

object Intents {
    object Actions {
        const val INSTALL = "app.involvexcord.manager.INSTALL"
    }

    object Extras {
        const val VERSION = "version"
    }
}
